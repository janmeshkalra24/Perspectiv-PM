services:
  develop: &develop
    build: ./
    volumes:
     - .:/tmp/py-zoom-meeting-sdk
     - ./sample_program/out:/tmp/py-zoom-meeting-sdk/sample_program/out
    ports:
      - "3000:3000"
    environment:
      - ZOOM_APP_CLIENT_ID
      - ZOOM_APP_CLIENT_SECRET
      - MEETING_URLS